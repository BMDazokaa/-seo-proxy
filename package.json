{
  "name": "seo-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for DataForSEO API — used by Claude SEO Audit artifact",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
